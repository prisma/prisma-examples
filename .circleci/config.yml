version: 2
jobs:
  readmes:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run: yarn
      - run: yarn build

  node:
    docker:
      - image: circleci/node
    working_directory: node
    steps:
      - checkout
      - run: cd cli-app && yarn

workflows:
  version: 2
  build:
    jobs:
      - readmes
      - node