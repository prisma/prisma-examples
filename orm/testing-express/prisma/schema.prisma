generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "sqlite"
  url      = env("DB_URL")
}

model User {
  id    Int     @id @default(autoincrement())
  name  String?
  email String  @unique
}
