generator client {
  provider   = "prisma-client"
  output     = "./generated"
  engineType = "client"
}

datasource db {
  provider = "postgresql"
}

model Quotes {
  id        Int      @id @default(autoincrement())
  quote     String
  createdAt DateTime @default(now())
}
