import { describe, test } from 'vitest'

describe('databases/postgresql-supabase', () => {
  test.skip('disabled - requires Supabase setup', async () => {
    // This test is disabled as it requires Supabase setup
  })
})
