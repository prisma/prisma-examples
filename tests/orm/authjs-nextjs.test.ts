import { describe, test } from 'vitest'

describe('orm/authjs-nextjs', () => {
  test.skip('disabled - npm install has peer dependency conflicts', async () => {
    // This example has peer dependency issues
  })
})
