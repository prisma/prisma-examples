import { describe, test } from 'vitest'

describe('orm/nuxt-prisma-module', () => {
  test.skip('disabled - requires special setup', async () => {
    // This test is disabled as it requires special setup
  })
})
