import { describe, test } from 'vitest'

describe('orm/postgis-express', () => {
  test.skip('disabled - requires PostGIS extension', async () => {
    // This example requires PostGIS which is not available in @prisma/dev
  })
})
