import { describe, test } from 'vitest'

describe('orm/hapi-graphql', () => {
  test.skip('disabled - prisma generate fails', async () => {
    // This example has issues with prisma generate
  })
})
